"""Typed evaluation harness for the CDSS pipeline.

Pure metric functions live in `eval_metrics.py`. The orchestrator in
`scripts/run_eval.py` ties them together against the JSON benchmark suites.
"""
